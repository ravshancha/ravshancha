# UI action icon PNG assets

- Source: `videoframe_4432.png`
- Count: 150 icons
- Canvas: 64 × 64 px
- Format: transparent PNG with a white icon
- Naming: `ui-action-rXX-cXX.png`

Rows are numbered from top to bottom and columns from left to right. Keep the same row/column names when animated files are extracted from the video so the static and animated variants can be paired directly.

The overview contact sheet is stored one directory above as `overview.png`.
